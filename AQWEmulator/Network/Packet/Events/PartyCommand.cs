using JSON;
using JSON.Command;
using World;
using World.Helper;
using World.Packet;
using World.Users;

namespace Packet.Events
{
    public class PartyCommand : IPacketEvent
    {
        public void Dispatch(User user, int room, string[] parameters)
        {
            
        }
    }
}
